Validation scope for this change:
- Existing `/cron/premarket` gate logic is unchanged.
- Existing `modules/premarket.js` is unchanged.
- Existing Render service and environment variables are unchanged.
- Existing ChatGPT 07:30 automation is unchanged.
- Only the GitHub trigger retries and their configuration check/documentation are added.
